<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.red.domain.boards.BoardsDao">
	
	<insert id="insert">
		insert into boards(id, title, content, userId, createdAt) values(nextval(boards_seq), #{title}, #{content}, #{userId}, sysdate());
	</insert>

	<select id="findById" resultType="site.metacoding.red.domain.boards.Boards">
	
	</select>
	
	<select id="findAll" resultType="site.metacoding.red.domain.boards.Boards">
		
	</select>
	
	<update id="update">
	
	</update>
	
	<delete id="delete">
	
	</delete>

</mapper>